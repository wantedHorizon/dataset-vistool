{
  "name": "flickr8k-viz-backend",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "build": "echo 'nothing to build for the backend'",
    "lint": "echo 'no python linter configured yet'",
    "test": "pytest",
    "db:populate": ".venv/bin/python -m app.ingest",
    "db:drop": "rm -rf data/registry.json data/datasets data/flickr8k.db"
  }
}
